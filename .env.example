# Discord Developer Portal (https://discord.com/developers/applications)
DISCORD_TOKEN=

# Database (optional, defaults shown below; only sqlite is supported for now)
# Note: when running with Docker Compose, the path is fixed to the /data
# volume by the compose file and these values are ignored

#DATABASE_DIALECT=sqlite
#DATABASE_URL=file:./data/bot.sqlite3
